version: '3'

services:
  varnish_nginx:
    ports:
      - "80:80"
      - "443:443"
  database:
    ports:
      - "3306:3306"
  rabbitmq:
    ports:
      - "15672:15672"
  mailhog:
    ports:
      - "1080:80"
